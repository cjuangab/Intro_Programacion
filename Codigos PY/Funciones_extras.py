input("Mensaje que quiero mostrar por consola al solicitar el ingreso de datos")
# Nos devuelve siempre un String o cadena de caracteres

int(...) #Convierte a entero a un elemento que pasemos por el argumento
float(...) #Convierte a flotante a un elemento que pasemos por el argumento


print(variable) # Imprime por consola la variable o dato introducida en el argumento
#puedo concadenar una cadena o string con una variable con la forma:
print(f'Mensaje o cadena que quiero concadenar: {variable}')


type(...) # Devuelve el tipo de dato introducido en el argumento
len(...) #Devuelve el largo de una cadena, lista, tupla o similar
dir(...) #Devuelve un listado de funciones y metodos que se pueden realizar con elemento